<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="0dp"
    android:paddingLeft="0dp"
    android:paddingRight="0dp"
    android:paddingTop="0dp"
    tools:context=".TransportResultActivity" >

    <include
        android:id="@+id/include1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        layout="@layout/header" />

    <LinearLayout
        android:id="@+id/transport_details_wrapper"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/include1"
        android:orientation="vertical" >  
    </LinearLayout>

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="72dp"
        android:orientation="horizontal" >

        <Button
            android:id="@+id/transport_details_pref"
            style="@style/HeaderTheme"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_marginLeft="25dp"
            android:background="@drawable/standard_button"
            android:drawableLeft="@drawable/arrow_left"
            android:drawablePadding="5dip"
            android:height="35dp"
            android:paddingLeft="5dp"
            android:paddingRight="5dp"
            android:onClick="prefPage"
            android:text="@string/transport_details_pref"
            android:width="130dp" />

        <Button
            android:id="@+id/transport_details_next"
            style="@style/HeaderTheme"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_marginRight="25dp"
            android:background="@drawable/standard_button"
            android:drawablePadding="5dip"
            android:drawableRight="@drawable/arrow_right"
            android:height="35dp"
            android:paddingLeft="5dp"
            android:paddingRight="5dp"
            android:onClick="nextPage"
            android:text="@string/transport_details_next"
            android:width="130dp"  />
    </RelativeLayout>

    <include
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        layout="@layout/footer" />

</RelativeLayout>